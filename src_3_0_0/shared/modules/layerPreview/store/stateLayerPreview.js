/**
     * @typedef {object} stateLayerPreview
     * @description creates a preview of a layer.
     * @property {Boolean} center=null center coordinates of the preview
     * @property {String} zoomLevel=null zoom level of the preview
     */
const state = {
    center: null,
    zoomLevel: null
};

export default state;
