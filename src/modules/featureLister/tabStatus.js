/**
 * Tab status constants to define the status of a tab in the feature lister.
 * @readonly
 * @enum {string}
 */

const tabStatus = {
    ENABLED: "enabled",
    ACTIVE: "active",
    DISABLED: "disabled"
};

export default tabStatus;
