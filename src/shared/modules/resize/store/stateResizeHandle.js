/**
* @typedef {Object} stateDimensions
 * @description Represents the dimensions of the resizable element.
 * @property {Number} [currentWidth=0] Initial width of the resizable element in pixels.
 */
const state = {
    mainMenuWidth: 0,
    secondaryMenuWidth: 0
};

export default state;
